{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "build-server-image": "docker build -t yassineldeeb/graphql:1 -f ./Dockerfile ."
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
